public interface Constants {

    // Will contain a list of possible identifiers based on the file type, which would be used as index before which code can be generated automatically.
    // Depending on the file type, this identifier can be either the last curly brace of a class or the last tag in an xml/xsd file, etc

    String XSD_SCHEMA_BEFORE_STRING = "";

}
